# Server configuration. Copy to .env and fill in the provider you use.
#
# This file is for the SERVER only — the client never needs an API key.
# server/src/jobstitch_server/resources/models.toml has one [provider] table
# naming the key/endpoint variables below, shared by the three models
# (summary, cv, highlight) — fill in the one provider you use.

# --- LLM provider -------------------------------------------------------
# One provider, one key/endpoint pair — any OpenAI-compatible /chat/completions
# API works. Shipped pointing at Alibaba DashScope (Qwen); to switch, just
# replace the two values below, e.g.:
#   OpenAI:    https://api.openai.com/v1
#   DeepSeek:  https://api.deepseek.com/v1
#   Ollama:    http://localhost:11434/v1  (MODEL_API_KEY can be any string —
#              Ollama ignores it, but the OpenAI client requires one)
MODEL_API_KEY=
MODEL_BASE_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1

# --- Per-model overrides -------------------------------------------------
# Override model name, temperature, thinking mode or JSON output mode for one
# role without editing models.toml — handy for Docker. Uncomment only what
# you need; anything unset keeps models.toml's value. Roles: SUMMARY, CV,
# HIGHLIGHT. thinking is "auto" | "on" | "off". structured_output is
# "json_schema_strict" | "json_schema" | "json_object" | "none".
# JOBSTITCH_SUMMARY_MODEL=
# JOBSTITCH_SUMMARY_TEMPERATURE=
# JOBSTITCH_SUMMARY_THINKING=
# JOBSTITCH_SUMMARY_STRUCTURED_OUTPUT=
# JOBSTITCH_CV_MODEL=
# JOBSTITCH_CV_TEMPERATURE=
# JOBSTITCH_CV_THINKING=
# JOBSTITCH_CV_STRUCTURED_OUTPUT=
# JOBSTITCH_HIGHLIGHT_MODEL=
# JOBSTITCH_HIGHLIGHT_TEMPERATURE=
# JOBSTITCH_HIGHLIGHT_THINKING=
# JOBSTITCH_HIGHLIGHT_STRUCTURED_OUTPUT=

# --- Server ------------------------------------------------------------------
# Require this bearer token on every /v1 call. Unset means the server is open
# to anyone who can reach it.
# JOBSTITCH_API_TOKEN=
# Requests in flight before the server answers 429 (default 10).
# JOBSTITCH_MAX_CONCURRENT_JOBS=10
# Wall clock for one CV run before it gives up (default 1200s).
# JOBSTITCH_REQUEST_BUDGET_SECONDS=1200
# Replacement prompts / template / models.toml (default: baked into the image).
# JOBSTITCH_RESOURCES=/path/to/resources
# Where per-request scratch directories are made (default: the system temp dir).
# JOBSTITCH_WORK_DIR=/tmp/jobstitch
# Port uvicorn listens on (default 8080; most platforms inject this).
# PORT=8080

# --- Client ------------------------------------------------------------------
# The client reads these too, but jobstitch.toml next to the binary is the
# usual way (see client/README.md).
# JOBSTITCH_API_URL=http://localhost:8080
